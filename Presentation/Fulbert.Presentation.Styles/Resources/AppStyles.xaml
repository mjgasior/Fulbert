<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <SolidColorBrush Color="#fff" x:Key="APP_WHITE"/>
    <SolidColorBrush Color="#efefef" x:Key="APP_ALMOST_WHITE"/>
    <SolidColorBrush Color="#e0e0e0" x:Key="APP_BRIGHT_GRAY"/>
    <SolidColorBrush Color="#909090" x:Key="APP_GRAY"/>
    <SolidColorBrush Color="#191919" x:Key="APP_ALMOST_BLACK"/>

    <FontFamily x:Key="APP_FONT_FAMILY">Segoe UI Light</FontFamily>
    <sys:Double x:Key="APP_FONT_SIZE">16</sys:Double>

    <Style TargetType="{x:Type TextBox}">
        <Setter Property="Margin" Value="10"/>
        <Setter Property="FontSize" Value="{StaticResource APP_FONT_SIZE}"/>
        <Setter Property="FontFamily" Value="{StaticResource APP_FONT_FAMILY}"/>
        <Setter Property="Background" Value="{StaticResource APP_BRIGHT_GRAY}"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>

    <Style TargetType="{x:Type Label}">
        <Setter Property="FontFamily" Value="{StaticResource APP_FONT_FAMILY}"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="Margin" Value="10"/>
        <Setter Property="Background" Value="{StaticResource APP_BRIGHT_GRAY}"/>
    </Style>

    <Style TargetType="{x:Type Label}" x:Key="labelAppHeader">
        <Setter Property="Background" Value="{StaticResource APP_GRAY}"/>
        <Setter Property="Foreground" Value="{StaticResource APP_ALMOST_WHITE}"/>
        <Setter Property="FontFamily" Value="{StaticResource APP_FONT_FAMILY}"/>
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="Padding" Value="16"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style TargetType="{x:Type Label}" x:Key="labelHeaderStyle" BasedOn="{StaticResource labelAppHeader}">
        <Setter Property="Background" Value="{StaticResource APP_WHITE}"/>
        <Setter Property="Foreground" Value="{StaticResource APP_ALMOST_BLACK}"/>
        <Setter Property="FontSize" Value="{StaticResource APP_FONT_SIZE}"/>
    </Style>

    <Style TargetType="{x:Type Button}">
        <Setter Property="FontFamily" Value="{StaticResource APP_FONT_FAMILY}"/>
        <Setter Property="Background" Value="{StaticResource APP_BRIGHT_GRAY}"/>
        <Setter Property="FontSize" Value="{StaticResource APP_FONT_SIZE}"/>
        <Setter Property="Margin" Value="10"/>
    </Style>

</ResourceDictionary> 